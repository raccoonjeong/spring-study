export const meta = {
  title: "버튼",
  subtitle: "Button",
  order: 1,
  group: "Components",
};
import Button from "@/components/Button";

# 버튼 - Button

```jsx
import Button from "@/components/Button";
```

## /components/Button.jsx

```jsx
import clsx from "clsx";

export default function Button({
  icon,
  primary,
  full,
  size = "md",
  children,
  ...rest
}) {
  return (
    <button
      className={clsx(
        "cursor-pointer border border-[#0063ba] font-semibold flex items-center",
        primary ? "bg-[#0063ba] text-white" : "text-[#0063ba] bg-white",
        full ? "flex w-full justify-center" : "",
        size === "lg" ? "px-6 h-[60px] text-lg" : "px-4 h-[38px] text-sm "
      )}
      {...rest}
    >
      {icon && <div>{icon}</div>}
      <div>{children}</div>
    </button>
  );
}
```

## 기본

<Button>버튼</Button>
```jsx
<Button>버튼</Button>
```

## Primary

<Button primary>버튼</Button>
```jsx
<Button primary>버튼</Button>
```

## Full

<div className="space-y-4">
  <Button full>full 버튼</Button>
  <Button full primary>
    full primary 버튼
  </Button>
</div>
```jsx
<div className="space-y-4">
  <Button full>full 버튼</Button>
  <Button full primary>
    full primary 버튼
  </Button>
</div>
```

## Size

<div className="flex space-x-4 items-center">
  <Button size="md">사이즈 md</Button>
  <Button size="lg">사이즈 lg</Button>
</div>

```jsx
<div className="flex space-x-4 items-center">
  <Button size="md">사이즈 md</Button>
  <Button size="lg">사이즈 lg</Button>
</div>
```
