<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hye.approvals.mapper.LoginMapper">


	<select id="login" parameterType="com.hye.approvals.dto.LoginDTO"
						resultType="com.hye.approvals.dto.UserDTO">
		select
			user_id,
			emp_name,
			emp.position_cd,
            position_name,
            level_no
		from
			approval_emp emp
		inner join positions po
			on emp.position_cd = po.position_cd
		where
			user_id = #{userId} and user_pw = #{userPw}
	</select>

</mapper>